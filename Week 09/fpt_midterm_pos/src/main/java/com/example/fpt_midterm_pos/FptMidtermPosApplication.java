package com.example.fpt_midterm_pos;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class FptMidtermPosApplication {

	/**
	 * This method is the entry point of the Spring Boot application.
	 * It initializes and runs the application using the provided main class.
	 *
	 * @param args The command-line arguments provided when the application is started.
	 */
	public static void main(String[] args) {
		SpringApplication.run(FptMidtermPosApplication.class, args);
	}
}